# edu-courses-interface


### Install and run
`git clone https://github.com/saequus/edu-courses-interface.git`

`cd edu-courses-interface`

`cd frontend/`

`npm install`

`npm run dev`

The application should have started running on http://localhost:8080
